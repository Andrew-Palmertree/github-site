# Web framework
Flask==3.0.3

# HTTP requests to Splunk HEC (with SSL support)
requests==2.32.3

# For working with environment variables
python-dotenv==1.0.1

# For running in production on Render
gunicorn==23.0.0

# For advanced SSL/TLS handling (needed for loading custom certs)
cryptography==43.0.1

# Optional, but recommended to avoid SSL warnings when using requests
urllib3==2.2.3
