# Web framework
Flask==3.1.2

# HTTP requests
requests==2.32.3

# For working with environment variables
python-dotenv==1.0.1

# For running in production
gunicorn==23.0.0

# Profanity detection (lightweight + Render-compatible)
better-profanity==0.7.0
clean-text==0.6.0

# Utilities to avoid build errors on Render
setuptools>=75.0.0
wheel>=0.45.0
